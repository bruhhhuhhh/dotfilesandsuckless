#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='\W  '
alias ofm="ranger"
alias omusic="cmus"
alias q="exit"
alias c="clear"
alias t="timedatectl"
alias whereami="pwd"
alias untar="tar -xvf"
alias sudo="doas"
